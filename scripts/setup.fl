/* script_type = "setup" */
val builtins = import "#builtins" .all


val objectID = 0a


fun val classCreationOverride(name, bases, attributes)
{
    return builtins.classOf(name, bases, attributes)
}
